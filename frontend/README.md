# OrionScheduler Console

This is the Next.js observability console for OrionScheduler.

Use the root `README.md` for setup, deployment variables, and demo instructions. The deployed console must set `NEXT_PUBLIC_API_URL`, `NEXT_PUBLIC_WS_URL`, and `NEXT_PUBLIC_ADMIN_TOKEN` for the one-click public recruiter demo.
